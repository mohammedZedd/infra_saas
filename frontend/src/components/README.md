# components

All reusable React components, organised by domain.

## Sub-directories

| Directory | Description |
|-----------|-------------|
| `auth/` | Authentication forms and layout (login, register) |
| `dashboard/` | Project dashboard UI (project cards, new-project modal, empty state) |
| `canvas/` | Visual infrastructure canvas editor — canvas, panels, modals, nodes |
| `landing/` | Public marketing landing page sections (hero, features, CTA) |
| `layout/` | Shared navigation and footer components |
| `project/` | Project detail view tabs (overview, runs, code, git, security, etc.) |
| `ui/` | Generic, reusable UI primitives (e.g. FeatureCard) |

