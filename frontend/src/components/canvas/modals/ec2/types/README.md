# components/editor/modals/ec2/types

TypeScript types and default values for the EC2 configuration wizard.

## Files

| File | Description |
|------|-------------|
| `ec2-config.ts` | `EC2FullConfig` interface (maps to `aws_instance` Terraform resource), `defaultEC2Config` constant, `EC2Step` type, and `EC2_STEPS` array |

