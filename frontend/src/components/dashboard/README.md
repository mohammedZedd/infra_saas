# components/dashboard

Components used on the project dashboard page.

## Files

| File | Description |
|------|-------------|
| `EmptyState.tsx` | Illustration and CTA shown when the user has no projects |
| `NewProjectModal.tsx` | Modal form for creating a new infrastructure project |
| `ProjectCard.tsx` | Card component displaying a project summary in the grid |

