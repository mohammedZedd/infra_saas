# data

Reserved for application-level static data files.

Currently empty — domain-specific data (e.g. EC2 AMIs, instance types) lives alongside
the components that use it (`components/editor/modals/ec2/data/`).

